#pragma warning(disable : 4996)
using namespace std;
#include <iostream>
#include <string>
#include <cstring>
#include <algorithm>
#include <vector>
#include <stack>
#include <queue>
#include <deque>
#define _CRT_SECURE_NO_WARNINGS

/*
struct info{
	int number; //제품번호
	int price; //선풍기 가격
	int time; //추가비용 발생 시간 간격
	int cost; //추가비용 초기값

	int totalPrice = 0; //최종 가격
};


int main() {
	int N, Q; //선풍기 개수, 집까지 걸리는 시간
	scanf("%d %d", &N, &Q);

	info infos[10010];
	int p, k, c;
	for (int i = 0; i < N; i++) {
		scanf("%d %d %d", &p, &k, &c); //선풍기 가격, 시간 간격, 초기값
		infos[i].number = i;
		infos[i].price = p;
		infos[i].time = k;
		infos[i].cost = c;
	}


	for (int i = 0; i < N; i++) {
		if (infos[i].time >= Q) { 
			infos[i].totalPrice = 
		
		}
		else {
		
		
		}





	}





	//프린트
	//제품번호에 +1, 해당제품번호의 토탈프라이스


	return 0;
}*/