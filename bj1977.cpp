#pragma warning(disable : 4996)
using namespace std;
#include <iostream>
#include <string>
#include <cstring>
#include <algorithm>
#include <vector>
#include <stack>
#include <queue>
#include <deque>
#include <cmath>
#define _CRT_SECURE_NO_WARNINGS


/*
int main() {

	int m, n;
	scanf("%d %d", &m, &n); //m<= <=n

	int count = 0;
	int sum = 0;
	int min_val = 10000;
	int i;

	for (i = 1; i <= 100; i++) {
		if (i * i >= m && i * i <= n) {
			sum += i * i;
			count++;
			min_val = min(min_val, i * i);
		}
	}

	if (count == 0) {
		printf("-1\n");
	}
	else {
		printf("%d\n%d", sum, min_val);
	}
	


	return 0;
}*/