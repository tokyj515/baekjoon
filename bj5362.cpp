#pragma warning(disable : 4996)
using namespace std;
#include <iostream>
#include <iomanip>
#include <string>
#include <cstring>
#include <algorithm>
#include <vector>
#include <stack>
#include <queue>
#include <deque>
#include <functional>
#include <cmath>
#include <map>
#include <set>
#define _CRT_SECURE_NO_WARNINGS
#define INF 987654321
#define ll long long
typedef pair<int, int> PII;

/*
string s;
vector<string> v;
vector<string> res;

int main() {
	getline(cin, s);

	string temp = "";
	for (int i = 0; i < s.size(); i++) {
		if (s[i] != ' ') {
			temp += s[i];
		}
		else {
			v.push_back(temp);
			temp = "";
		}
	}
	

	for (int i = 0; i < v.size(); i++) {
		string s = v[i];

		if (s.substr(s.size() - 5, 5) == "iiing") {
			s.erase(s.size() - 5, 5);
			s = s + "th";
		} 
		else if (s.substr(0, 5) == "iiing") {
			s.erase(0, 5);
			s = "th" + s;
		}

		res.push_back(s);
	}


	for (int i = 0; i < res.size(); i++) {
		cout << res[i] << " ";

		if (i == res.size() - 1) {
			cout << 
		}
	}


	return 0;
}*/