#pragma warning(disable : 4996)
using namespace std;
#include <iostream>
#include <string>
#include <cstring>
#include <algorithm>
#include <vector>
#include <stack>
#include <queue>
#include <deque>
#include <cmath>
#define _CRT_SECURE_NO_WARNINGS

/*

string A, B;

void zero(string a, string b) {
	long long gap;
	
	if (a.size() > b.size()) {
		gap = a.size() - b.size();

		while (gap--) {
			b = "0" + b;
		}
		A = a;
		B = b;
		printf("%s %s\n", A.c_str(), b.c_str());
	}
	else if (a.size() < b.size()) {
		gap = b.size() - a.size();

		while (gap--) {
			a = "0" + a;
		}
		A = a;
		B = b;
		printf("%s %s\n", A.c_str(), b.c_str());
	}
	else {//a.size() == b.size()
		A = a;
		B = b;
		printf("%s %s\n", A.c_str(), b.c_str());
	}


}
string add(string a, string b) {
	string ans = "";
	long long len = a.size() - 1;
	int carry = 1;

	for (int i = len; i >= 0; i--) {
		
	
	
	
	}





	return ans;
}

int main() {
	cin >> A >> B;

	zero(A, B);

	string ans = add(A, B);

	printf("%s", ans.c_str());


	return 0;
}*/