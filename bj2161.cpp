#pragma warning(disable : 4996)
using namespace std;
#include <iostream>
#include <string>
#include <cstring>
#include <algorithm>
#include <vector>
#include <stack>
#include <queue>
#include <deque>
#include <functional>
#include <cmath>
#define _CRT_SECURE_NO_WARNINGS
/*

int main() {
	queue<int> q;
	int n;
	scanf("%d", &n);

	for (int i = 1; i <= n; i++) {
		q.push(i);
	}

	while (!q.empty()) {
		printf("%d ", q.front());
		q.pop();

		int temp = q.front();
		q.pop();
		q.push(temp);


		if (q.size() == 1) {
			printf("%d", q.front());
			break;
		}
	
	
	
	}




	return 0;
}*/