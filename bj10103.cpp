#pragma warning(disable : 4996)
using namespace std;
#include <iostream>
#include <string>
#include <cstring>
#include <algorithm>
#include <vector>
#include <stack>
#include <queue>
#include <deque>
#include <functional>
#include <cmath>
#define _CRT_SECURE_NO_WARNINGS

/*

int main() {
	int n;
	int a, b;
	int aScore = 100;
	int bScore = 100;


	scanf("%d", &n);
	
	while (n--) {
		scanf("%d %d", &a, &b);

		if (a > b) {
			bScore -= a;
		}
		else if (a < b) {
			aScore -= b;
		}
		else {
			continue;
		}

	}

	printf("%d\n%d", aScore, bScore);

	return 0;
}*/