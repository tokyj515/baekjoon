#pragma warning(disable : 4996)
using namespace std;
#include <iostream>
#include <string>
#include <cstring>
#include <algorithm>
#include <vector>
#include <stack>
#include <queue>
#include <deque>
#include <functional>
#include <cmath>
#include <map>
#define _CRT_SECURE_NO_WARNINGS
#define INF 987654321
#define ll long long
typedef pair<int, int> P;


/*
int main() {
	int t;
	scanf("%d", &t);


	while (t--) {
		int n, m, k, d;
		scanf("%d %d %d %d", &n, &m, &k, &d);
		

		ll A, B;
		//A = K*B

		B = (2 * d) / (m * n * (k * (m - 1) + m * (n - 1)));
		A = B * k;


		ll total = (m * n * (m - 1) / 2 * A ) + (n * (n - 1) / 2 * m * m * B);

		

		if (total > 0) {
			printf("%d\n", total);
		}
		else {
			printf("-1\n");
		}
	
	}



	return 0;
}*/