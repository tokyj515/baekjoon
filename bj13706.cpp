#pragma warning(disable : 4996)
using namespace std;
#include <iostream>
#include <string>
#include <cstring>
#include <algorithm>
#include <vector>
#include <stack>
#include <queue>
#include <deque>
#include <functional>
#include <cmath>
#define _CRT_SECURE_NO_WARNINGS

/*
int main() {
	int n;
	scanf("%d", &n);

	int start = 0;
	int end = n;

	while (start <= end) {
		int mid = (start + end) / 2;
		int target = mid * mid;

		if (target == n) {
			printf("%d\n", mid);
		}
		else if (target < n) {
			start = mid + 1;
		}
		else if (target > n) {
			end = mid - 1;
		}
		
	
	}


	return 0;
}*/