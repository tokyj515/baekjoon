#pragma warning(disable : 4996)
using namespace std;
#include <iostream>
#include <string>
#include <cstring>
#include <algorithm>
#include <vector>
#include <stack>
#include <queue>
#include <deque>
#define _CRT_SECURE_NO_WARNINGS

/*
//int map[100][100] = { 1 };

int main() {
	int t;
	scanf("%d", &t);

	while (t--) {
		int result = 0;
		int H, W, N;
		scanf("%d %d %d", &H, &W, &N);

	
		if (N % H == 0) {
			result = H * 100 + (N / H);
		}
		else {
			result = (N % H) * 100 + (N / H) + 1;
		}
		

		printf("%d\n", result);
	}

	return 0;
}*/