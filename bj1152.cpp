#pragma warning(disable : 4996)
using namespace std;
#include <iostream>
#include <string>
#include <cstring>
#include <algorithm>
#include <vector>
#include <stack>
#include <queue>
#include <deque>
#define _CRT_SECURE_NO_WARNINGS

/*

int main() {

	string str;
	getline(cin, str);

	

	if (str.empty()) {
		printf("0");
		return 0;
	}


	int count = 1;

	for (int i = 0; i < str.size(); i++) {
		if (str[i] == ' ') {
			count++;
		}
	}

	if (str[0] == ' ') {
		count--;
	}
	if (str[str.size()-1] == ' ') {
		count--;
	}


	printf("%d\n", count);

	return 0;

}*/