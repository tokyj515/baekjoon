
#pragma warning(disable : 4996)
using namespace std;
#include <iostream>
#include <string>
#include <cstring>
#include <algorithm>
#include <vector>
#include <stack>
#include <queue>
#include <deque>
#include <cmath>
#define _CRT_SECURE_NO_WARNINGS


/*
int main() {
	int num[1000];
	int n;
	double anspercent = 0;

	int t;
	scanf("%d", &t);

	int i, j, k;
	
	for(j=0; j<t; j++) {
		int count = 0;
		int avg = 0;

		scanf("%d", &n); 

		
		for (i = 0; i < n; i++) {
			scanf("%d", &num[i]);
			avg += num[i];
		}
		avg = avg / n;


		for (i = 0; i < n; i++) {
			if (num[i] > avg) count++;
		}
	
		anspercent = (double)count / n * 100;


		cout << fixed;
		cout.precision(3);
		cout << anspercent << "%" << "\n";

	
	}





	return 0;
}*/