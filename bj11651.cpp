

#pragma warning(disable : 4996)
using namespace std;
#include <iostream>
#include <string>
#include<cstring>
#include<algorithm>
#include<vector>
#define _CRT_SECURE_NO_WARNINGS

/*
bool compare(pair<int, int> a, pair<int, int> b) {//y¡¬«•±‚¡ÿ -> x¡¬«•±‚¡ÿ
	if (a.second == b.second) {
		return a.first < b.first;
	}
	else {
		return a.second < b.second;
	}
}



int main() {

	int n;
	scanf("%d", &n);
	vector<pair<int, int>>coor(n); //x, y


	int i;
	for (i = 0; i < n; i++) {
		scanf("%d %d", &coor[i].first, &coor[i].second);
	}

	sort(coor.begin(), coor.end(), compare);

	for (i = 0; i < n; i++) {
		printf("%d %d\n", coor[i].first, coor[i].second);
	}



	return 0;
}*/