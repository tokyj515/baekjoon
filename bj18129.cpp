#pragma warning(disable : 4996)
using namespace std;
#include <iostream>
#include <string>
#include <cstring>
#include <algorithm>
#include <vector>
#include <stack>
#include <queue>
#include <deque>
#define _CRT_SECURE_NO_WARNINGS

/*
int main() {
	bool upper[30]; //대문자
	bool lower[30]; //소문자

	string ans = "";
	string s;
	int k;
	cin >> s >> k;


	int count = 0;
	int i = 0;
	while (i < s.size()) {
		char c = s[i];
		if (upper[c - 'A'] == false || lower[c - 'a'] == false) {
			count++;

			if (c != s[i + 1]) {
				if (count >= k) {
					ans += "1";
				}
				else {
					ans += "0";
				}
			}
		}
		else {
			
		
		}
	
	
	}



	cout << ans;


	return 0;
}*/