#pragma warning(disable : 4996)
using namespace std;
#include <iostream>
#include <string>
#include <cstring>
#include <algorithm>
#include <vector>
#include <stack>
#include <queue>
#include <deque>
#include <functional>
#include <cmath>
#include <map>
#include <set>
#define _CRT_SECURE_NO_WARNINGS
#define INF 987654321
#define ll long long
typedef pair<int, int> PII;

/*

vector<char> jong = {'q', 'a', 'z', 'w', 's', 'x', 'e', 'd', 'c', 'r', 'f', 'v', 't', 'g'};
vector<char> nojong = {'b', 'y', 'h', 'n', 'u','j', 'm', 'i', 'k', 'o', 'l', 'p'};

int main() {
	int n;
	string s;

	cin >> n >> s;

		
	for (int i = 0; i < jong.size(); i++) {
		if (s[s.size() - 1] == jong[i]) {
			cout << "1" << "\n";
			return 0;
		}
	}

	for (int i = 0; i < nojong.size(); i++) {
		if (s[s.size() - 1] == nojong[i]) {
			cout << "0" << "\n";
		}
	}
	
	

	return 0;
}*/